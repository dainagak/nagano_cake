<h1>商品詳細画面</h1>
<% @.each do |item| %>
  <%= item.title %>
  <%= item.introduction %>
  <%= item.price %>
  <%= item.image %>
<% end %>
  <%= link_to "編集", edit_admin_item_path(@item.id), class:"btn btn-success"%>